{
    "devDependencies": {
        "cypress": "^8.3.1",
        "start-server-and-test": "^1.14.0"
    },
    "scripts": {
        "cy:open": "cypress open",
        "cy:version": "cypress version",
        "cy:verify": "cypress verify",
        "cy:run": "cypress run",
        "cypress:run": "start-server-and-test start http://localhost:3000 cy:run"
    }
}
