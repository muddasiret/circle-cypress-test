{
    "devDependencies": {
        "cypress": "^8.3.1"
    },
    "scripts": {
        "cy:open": "cypress open",
        "cy:version": "cypress version",
        "cy:verify": "cypress verify",
        "cypress:run": "CYPRESS_BASE_URL=$npm_config_START_URL cypress run --browser chrome --headless"
    }
}
